{% extends 'base.html.twig' %}

{% block body %}
    <h1>Affiliate list</h1>

    <table>
        <thead>
            <tr>
                <th>Idaffiliate</th>
                <th>Url</th>
                <th>Email</th>
                <th>Token</th>
                <th>Isactive</th>
                <th>Createdat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for affiliate in affiliates %}
            <tr>
                <td><a href="{{ path('affiliate_show', { 'id': affiliate.idAffiliate }) }}">{{ affiliate.idAffiliate }}</a></td>
                <td>{{ affiliate.url }}</td>
                <td>{{ affiliate.email }}</td>
                <td>{{ affiliate.token }}</td>
                <td>{% if affiliate.isActive %}Yes{% else %}No{% endif %}</td>
                <td>{% if affiliate.createdAt %}{{ affiliate.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('affiliate_show', { 'id': affiliate.idAffiliate }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('affiliate_edit', { 'id': affiliate.idAffiliate }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('affiliate_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
