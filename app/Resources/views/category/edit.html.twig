{% extends 'base.html.twig' %}
{% block stylesheets %}
  <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
  <link href="{{ asset('css/bootstrap.css') }}" type="text/css" rel="stylesheet" />   
 {% endblock %}
{% block body %}

<div class="col-sm-6 col-sm-offset-4">
    <h1>Category edit</h1>

    {{ form_start(edit_form) }}
    <div class="form-group form-group-lg">
      <label class="col-sm-2 control-label text-center" for="lg">Name</label>
      <div class="col-sm-10">
        {{ form_widget(edit_form.name, {'attr': {'class': 'form-control'}}) }}
      </div>
    </div>
    <input type="submit" class="btn btn-info" value="Edit" /><br><br>
{{ form_end(edit_form) }}
   
            <a href="{{ path('category_index') }}"><button type="button" class="btn btn-info">Back to list</button></a><br><br>
    
            {{ form_start(delete_form) }}
                <input type="submit" class="btn btn-info"  value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
