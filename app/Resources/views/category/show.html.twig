{% extends 'base.html.twig' %}
{% block stylesheets %}
  <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
  <link href="{{ asset('css/bootstrap.css') }}" type="text/css" rel="stylesheet" />   
 {% endblock %}
{% block body %}

<div class = "container col-md-3 col-sm-offset-4">
    <h1>Category</h1>

    <table class="table table-reflow" id="joblist_table">
        <tbody>
            <tr>
                <th>Id category</th>
                <td>{{ category.idCategory }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>


            <a href="{{ path('category_index') }}"><button type="button" class="btn btn-success">Back to list</button></a>

            <a href="{{ path('category_edit', { 'id': category.idCategory }) }}"><button type="button" class="btn btn-success">Edit</button></a><br><br>
 
            {{ form_start(delete_form) }}
                <input type="submit" class="btn btn-success" value="Delete">
            {{ form_end(delete_form) }}
        </div>

{% endblock %}
