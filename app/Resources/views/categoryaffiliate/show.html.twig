{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categoryaffiliate</h1>

    <table>
        <tbody>
            <tr>
                <th>Idcategoryaffiliate</th>
                <td>{{ categoryaffiliate.idCategoryaffiliate }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categoryaffiliate_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('categoryaffiliate_edit', { 'id': categoryaffiliate.idCategoryaffiliate }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
